Hey there,

<%= @submission.name %> (<%= @submission.email %>) just submitted the form on janczizikow.com/contact. Here's what they had to say:
<%= @submission.message %>

The form was submitted at <%= @submission.created_at %>
